N = int(input())
#"for A in range(1, 10) means we are going to repeat 9 times, starting from 1, and assign each value to A.  
for p in range(1,10):
#also we need to print the process of multiplication table.
#In this case, we need to use 'str', which is used to convert things into a string.

  print(str(N) + " * " + str(p) +  " = " + str(N * p))
